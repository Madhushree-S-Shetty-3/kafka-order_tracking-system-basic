# kafka-order_tracking-system-basic
Learn Kafka topics, producer, consumer through a basic project
===
# Requirments

- Docker Desktop
  ```
  docker engine up and running
  ```

- Python Libraries
  ```
  pip3 install confluent-kafka
  ```
  For connecting to local kafka running on docker
